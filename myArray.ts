const superHeros: string[] = []

//Another way of syntax array declaration
const rashidAge: Array<number> = []

type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = []

superHeros.push("man")
allUsers.push({name: "ras", isActive: false})